# Post-release status — 2026-08-22

The public certificate scope remains exactly
`F(400000287233629)=104`, hence `sup_n F(n) >= 104`. It does not establish
world-record priority, unboundedness, global maximality, basin infinitude,
density, inverse-tree completeness, or a solution.

- Frozen release tag: `v3.0.0-f104`
- Tagged commit: `ee62d68d16f187b808c814108fd53881c4e3e614`
- Release: <https://github.com/ZackaryLoevseth/Erd-s-Problem-409/releases/tag/v3.0.0-f104>
- `FROZEN_V3_F104_SHA256SUMS` is a byte-exact copy of the tag's checksum
  ledger.
- The generic `SHA256SUMS` covers the forward current main tree after the
  README scope correction and excludes itself to avoid recursion.

The tag, release archive, certificate bytes, verification code, and scientific
result are unchanged. The separate accidental continuation and incomplete
inverse-fiber manuscript remain private, safe-stopped, and outside this public
repository correction.
