# Public-package provenance

The trajectory, two original evaluators, terminal-primality evidence,
requirements, packet README, and validation report are byte-for-byte copies
from the canonical private F=104 pointwise packet.

Public-release transformation:

- omit the private search-scope note and any inverse-enumeration claim;
- add this narrowed claim ledger;
- regenerate `MANIFEST.sha256` for the public directory;
- add the fresh third evaluator and mutation result under `validation/`.

No trajectory value, factorization, totient, transition, expected stopping
time, terminal prime, or original evaluator logic was altered.
